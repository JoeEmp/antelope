is_api_case: false

case:
  - set_step:
      name: "name"
      value: "melody"
  - if_step:
      - judge: '"{name}"== "Yang"'
  - else_step:
      - set_step:
          name: "device"
          value: "MP"
      - echo: "{device} {word}"
